# Airflow providers and utilities
# NOTE: pyspark NOT included here - Airflow submits jobs to the Spark cluster,
#       it does not need a local PySpark installation (saves 317MB of build time)
apache-airflow-providers-apache-spark==4.9.0
apache-airflow-providers-amazon==8.19.0
boto3==1.34.0
pandas==2.0.3
pyarrow==14.0.2
python-dotenv==1.0.0
pyyaml==6.0.1
requests==2.31.0
